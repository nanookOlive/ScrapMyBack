{% extends 'base.html.twig' %}

{% block title %}ScrapMyBack{% endblock %}

{%  block body %}
<table class="table">
    <thead>
      <tr>
        <th scope="col">Name</th>
        <th scope="col">Lien</th></th>
      </tr>
    </thead>
{% for item in data %}





    <tbody>
        
        

      <tr>
        <td>{{item.id}}</td>
        <td>{{item.name}}</td>
        
        <td>{{item.href}}</td>
        <td><a href={{path("app_show",{'id':item.id})}}><button type="button" class="btn btn-primary">Voir</button> </a></td>
      </tr>


      
    </tbody>{% endfor %}
  </table>

{% endblock %}
